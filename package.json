{
  "name": "martinez-corp-content-ops",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/functions": "^3.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^20.0.0"
  }
}
